add_newline = true
command_timeout = 200
format = '''
[┌──\(](bold blue)$username[㉿](bold blue)$hostname[\)─](bold blue)$directory$git_branch$git_status$custom
$character'''

[character]
success_symbol = '[└─\$ ](bold blue)'
error_symbol = '[└─\$ ](bold red)'

[username]
show_always = true
format = "[$user](bold red)"

[hostname]
ssh_only = false
format = "[$hostname](bold red)"

[directory]
truncation_length = 2
truncation_symbol = "…/"
style = "bold cyan"
format = '[\[](bold blue)[$path]($style)[\]](bold blue)'

[git_branch]
format = '[─\[](bold blue)[$branch]($style)[\]](bold blue)'
style = "italic cyan"

[git_status]
format     = '[─\[$all_status\]]($style)'
style      = "cyan"
ahead      = "+${count}"
diverged   = "±${ahead_count}-${behind_count}"
behind     = "-${count}"
conflicted = "="
up_to_date = ""
untracked  = "?"
modified   = "*"
stashed    = "$"
staged     = "+"
renamed    = ">"
deleted    = "x"

[custom.network_ip]
description = "Display active network interface and local IP address"
command = "~/.config/scripts/network-ip.sh"
when = "/usr/bin/test -x ~/.config/scripts/network-ip.sh"
style = "bold red"
format = '[─\[](bold blue)[$output]($style)[\]](bold blue)'
